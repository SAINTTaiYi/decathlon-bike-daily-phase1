# Journal — 2026-07-30

## Workshop Chrome MCP real-browser acceptance correction

- Recovered interrupted session on `fix/workshop-chrome-mcp-acceptance` at immutable baseline `fe3940308dee3f496474aa38c041354c504cd889`; no remote side effect was repeated.
- `rh-task reconcile` only finalized vanished historical workers and did not retry deployments, schema mutations or sends.
- Compared every `.orig`/`.rej` artifact against baseline/current source before deleting it.
- Recorded Termux limitations to avoid repeat failures: no `apply_patch`, `perl` or `rg`; complex shell regex/template editing is unsafe; use an idempotent Node replacement script and verify `git status` after failures.
- Implemented corrections for the three Chrome MCP findings: module-boundary leakage, obsolete Albert Sans preload warnings, and release notes beneath the dock.
- Updated closing-report image font readiness so generated images use only current self-hosted fonts rather than removed Albert Sans / Noto Serif assets.
- Added three source-contract regressions.
- Focused tests passed 26/26; complete Web regression passed 124/124; frozen offline install, root typecheck, Web build, 88 workflow policies and `git diff --check` passed.
- Post-change CodeGraph is up to date at 189 files / 2,273 nodes / 7,649 edges. The missing true pre-edit run, source-scanning tests and HTML/CSS coverage are explicit exceptions rather than being silently omitted.
- Public version remains V5.7.9. No Preview redeployment, Staging, Production or Production D1 mutation has occurred yet.
- Next queue: final commit → unchanged-version Preview fingerprint → clean root build → PR/Node 22 CI → protected Preview-only deployment → Chrome MCP re-acceptance.

## Workshop Chrome MCP acceptance correction — Preview deployed

- Functional commit `481367d0f1d4f705be51bb1c555125fed5bf8aa2`; unchanged-version Preview fingerprint `a83fe4f52ecb67dbbe0c2da7684fc916d14eeee8d259325b1d8f1edf07b6872b` across 365 versioned files.
- PR #98 passed Node 22 `secrets` and `verify` in run `30478399915`, then merged normally as `861e795773e3012912da45f9a9e35d15c63269e6`.
- Post-merge CI run `30478560899` succeeded for the exact merge SHA.
- Protected Preview-only workflow `30478746543` passed Free/no-billing/Preview-only confirmation, remote branch-head verification, frozen install, full test/typecheck/build, Worker bundling, Preview D1 and deployed identity checks.
- Preview D1 reported `No migrations to apply`; Staging, Production and Production D1 were not accessed or changed.
- Cloudflare Deployment `e7efdcae-c93f-4588-84f7-284dc70baf8a`; Worker Version `a965f15a-1e68-4a7f-845f-62701d8cd9c8` receives 100% traffic.
- Independent public checks confirmed live/ready/meta/Web at V5.7.9 / merge SHA, HTTP→HTTPS 308, HSTS, strict CSP, DENY, `nosniff`, restricted permissions and API `no-store, private`.
- Online CSS has 103 self-hosted font faces, zero forbidden system/generic runtime declarations and zero Albert references in HTML/CSS.
- Official `chrome-devtools-mcp@1.6.0` remains installed and origin-restricted, but ADB currently has no connected device and DevTools port 9222 is unavailable. Real-browser re-acceptance therefore requires one-time Android Wireless debugging reconnection; prohibited application-browser and Accessibility fallbacks were not used.

## Workshop native module stages — Preview deployed, awaiting human acceptance

- Replaced the rejected Story Scroll architecture with six native sticky cover/reveal stages. The new structure retains all scene components in the DOM, uses native document flow after a short stage interval, and updates dock state through passive `IntersectionObserver` tracking.
- Eliminated the old `ScrollTrigger` / scrub / pin / `pinSpacing:false` / ResizeObserver-refresh transition path. No wheel, touch or paging-key interception was added.
- Functional commit: `73e45ddf5f22438e86edd2001568a84d1891b8c9`; PR #100 passed `secrets` and `verify` in run `30493348339`; ordinary merge / actual Preview identity: `b2376f550c0a8b3af6b7d0ec05dff6526f24fde7`.
- Gates: targeted motion 6/6; related Workshop regression 25/25; final full `pnpm test`, typecheck and Web build passed. CodeGraph v1.5.0 is current at 189 files / 2,271 nodes / 7,648 edges. Markdown/PNG and source-scanning dependency limits are explicit coverage exceptions, not omitted checks.
- Added original self-hosted `workshop-slate-texture.png`, documented in `apps/web/public/images/ops/SOURCES.md`; source and deployed Preview copies have SHA-256 `97400d536978cddfe31ab84d4eeba6cf2e6c82a1b651949a9b19b1d2a4f7bae8`.
- Protected Preview workflow `30493526636` passed Free/no-billing/Preview-only guardrails, exact remote head, frozen install, all validation, Preview D1 migration, Worker/static deployment and identity checks. Worker Version: `2b3ab1ac-032c-466c-9ed9-784a506a99bf`.
- Independent public verification passed on https://bike-ops-preview.geeklightonefish.workers.dev: live, ready, meta, Web shell, texture asset, and security headers. Identity is V5.7.9 / `b2376f5` / Preview.
- Staging, Production and Production D1 remain untouched. Public version remains V5.7.9. The task now waits for human Preview interaction and visual acceptance; see `plan/WORKSHOP-OBSIDIAN-MOTION-PREVIEW-EVIDENCE.md`.

## Workshop native stages — human rejection and true parallax correction

- Human review rejected Preview identity `b2376f550c0a8b3af6b7d0ec05dff6526f24fde7`: the visible result was only a sticky stage with material texture and had no meaningful Obsidian Assembly About-style depth separation. The prior evidence file is now explicitly marked rejected rather than treated as accepted.
- Recovered the original public About HTML/JS/CSS evidence from local crawl caches. Verified formulas: normalized per-stage progress; title lines at 0/10vh/20vh ranges; foreground scale 1→1.2; background -20vh→+20vh; curve copy +75%; seven-word distance-based focus; transformed 1.5s character entry.
- User clarified that all six modules require the complete multi-layer mechanism and About-derived stage layout, not transition-only motion. Each module also requires its own original transparent Workshop object; business cards remain in the existing warm-white UI after the stage.
- Created true pre-change CodeGraph gate on branch `fix/workshop-obsidian-parallax` at administrative baseline `8fcb7b4a6240dd9936a70596777cc3f7be41727d`: 189 files / 2,271 nodes / 7,648 edges, up to date.
- Added `plan/WORKSHOP-OBSIDIAN-PARALLAX-SPEC.md`, a deterministic six-asset generator and six original transparent SVG object illustrations with recorded SHA-256 values. No reference-site asset or font is copied.
- Implemented continuous rAF stage progress and five independent moving layer groups for every mounted module. Pure progress and source contracts currently pass 11/11; full regression/build/CodeGraph/Preview delivery are still pending.
- Production, Staging and Production D1 remain untouched. Public version remains V5.7.9.

## Workshop true multilayer parallax — local gates passed

- Corrected implementation provides all six modules with continuous normalized stage progress, independent material/title/object/curve/trail layers, and deterministic transformed character entry; the original warm-white business UI follows each short stage.
- Targeted regressions pass 30/30; full repository `pnpm test` passed in detached task `workshop-obsidian-parallax-full-tests` (attempts 1, exit 0); repository typecheck passed in `workshop-obsidian-parallax-typecheck` (attempts 1, exit 0).
- Direct Web production build passed with 122 transformed modules. The root version-gated build remains intentionally pending until the functional commit can be registered by `pnpm version:preview` in a clean worktree; public version stays V5.7.9.
- CodeGraph v1.5.0 post-change sync handled 7 changed supported files (3 added, 4 modified, 99 nodes) and is up to date at 192 files / 2,299 nodes / 7,710 edges. It maps the changed JavaScript/JSX surface to `tests/module-stage-progress.test.mjs` and `tests/workspace-motion.test.mjs`.
- CSS, Markdown and SVG are explicit CodeGraph indexing exceptions. Coverage is provided by the direct production build, motion/source contract tests, deterministic regeneration, source/license records and six per-file SHA-256 values.
- No Preview has been redeployed yet. Staging, Production and Production D1 remain untouched. Next queue: bounded diff/secret audit → functional commit → unchanged-version Preview registration/root build → PR/CI → protected Preview-only deployment and independent verification.

## Workshop true multilayer parallax — Preview deployed, awaiting human acceptance

- Functional commit `a9cc870a3c56e335243b0d766a03f45966402f8a`; PR #102 CI run `30518797194` passed `secrets` and `verify`; ordinary merge / actual Preview identity is `73a02372619261fdba66b32765edb8f6fa011070`.
- Post-merge CI run `30518965973` passed full-history Gitleaks, all tests, typecheck, build and frontend policy checks. Public version remains V5.7.9; Preview fingerprint is `61c4f2c28036c8bf59d96c13f7142cc03429ff4c5ff6df209690acc9b5650659` across 376 files.
- Protected Preview-only workflow `30519156005` passed exact remote-head, Free/no-billing/Preview-only, frozen install, full validation, Worker bundling, Preview D1, deployment and identity gates. Preview D1 reported `No migrations to apply`.
- Cloudflare Deployment `f7d0a396-440e-4b27-acdb-4ced75398e0f`; Worker Version `00a40d0c-3acb-4ad2-a265-cb04a1fafd2e` (#102) carries 100% traffic.
- Independent public checks passed for live/ready/meta/Web, exact V5.7.9 / `73a0237` / Preview identity, six deployed SVG SHA-256 values, strict HTTPS security headers and HTTP 308.
- `browser-harness` is unavailable; prohibited application-browser and Accessibility fallbacks were not used. Final visual/scroll/interaction acceptance is therefore human-only.
- Staging, Production and Production D1 remain untouched. See `plan/WORKSHOP-OBSIDIAN-PARALLAX-PREVIEW-EVIDENCE.md`.
