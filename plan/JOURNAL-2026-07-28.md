# Journal · 2026-07-28

## Workshop mobile overview

- 恢复隐藏会话 `20260728T131750-bee3e9`，隔离目标 worktree `/data/data/com.termux/files/home/worktrees/workshop-mobile-overview`。
- 修改前 CodeGraph 初始化成功：183 files / 2,199 nodes / 7,530 edges。
- 用户确认设计文档为正式规范、图片仅作视觉基准；确认原创通用车辆 SVG；确认门禁后直接 PR + Preview，Production 禁止。
- 完成移动首屏组件、专用 CSS、自托管字体、原创车辆线稿、业务映射与专项测试。
- 修复本地依赖装配前置问题：最终 `CI=true pnpm install --offline --frozen-lockfile`，102 packages 全部复用、零下载、锁文件不变。
- 验证通过：179 测试、typecheck、88 workflow policies、web build、CF typecheck、worker bundle、静态几何审计。
- 修改后 CodeGraph：185 files / 2,234 nodes / 7,568 edges，up to date。
- 完整 build 仅待干净提交后的 Preview 指纹登记；版本继续为 V5.7.9。
- 下一步：功能提交 → Preview 登记与完整 build → browser-harness → PR/CI → Preview。
