# Journal · 2026-07-29

## Workshop 首屏参考图设计规格

- 用户要求逐项分析生成首屏效果图，确认每个组件、排版、位置、间隔和实际页面效果，并写入 Workshop 项目 Markdown。
- 决策：手机优先，同时覆盖平板和桌面；忠实还原视觉构图，但全部内容使用项目真实数据；本次仅文档，不修改页面代码、不部署。
- 最新基线：`feature/cloudflare-workers-d1` / `3c0f7b31466dba3c146164d368e08ec4a05a1bfc` / V5.7.9。
- 在隔离工作树 `~/worktrees/workshop-first-screen-layout-spec` 建立文档分支 `docs/generated-first-screen-layout-spec`。
- 修改前 CodeGraph v1.5.0 初始化并通过：185 files / 2,234 nodes / 7,614 edges，WAL，up to date。
- CodeGraph 映射首屏真实结构：`BrandHeader`、`StoreContextCard`、`ClosingStatusCard`、`SalesVehiclesPanel`、`OperationsIndex`、`PickupBoard`，以及 `ActionDock` 和 `useClosingWorkflow` 数据链。
- 参考图 `852 × 1705 px` 按 DPR 2 解释为约 `426 × 852.5 CSS px`；设计规格同时记录原图坐标和 CSS 坐标。
- 新增主规格：`plan/WORKSHOP-FIRST-SCREEN-REFERENCE-SPEC.md`。
- 规格覆盖全局 tokens、一级锚点、每个子元素、真实字段、状态矩阵、手机/平板/桌面重排、可访问性、禁止照搬的生成瑕疵与量化验收门禁。
- 后置 CodeGraph v1.5.0 同步并通过：185 files / 2,234 nodes / 7,614 edges，WAL，up to date；节点与前置一致，符合 Markdown-only 预期。
- 实施引用审计通过：规格提及的 13 个源码/样式/资源文件全部存在；11 个 KPI、工作流、用户及 Pickup 数据符号全部存在。
- 最终 `git diff --check` 通过；相对基线仅新增本日记和主规格两份 Markdown，共 546 行；无运行文件变更。
- CI 会由文档 PR 触发；Cloudflare Preview、Staging、Production 工作流均为 `workflow_dispatch`，本任务不触发部署。
- 本次无 JSX/CSS/API/数据库/迁移/版本/工作流/部署变更。下一步：创建文档 PR、等待 CI 并记录结果；不部署 Preview 或 Production。

## Workshop 首屏参考图实施

- 用户授权按照 `plan/WORKSHOP-FIRST-SCREEN-REFERENCE-SPEC.md` 修改项目代码，完成门禁、PR、合并并部署 Preview 供人工验收；不得修改公开版本号或触碰 Production。
- 实施分支：`feat/workshop-first-screen-reference`；起点为已合并规格基线 `802877eadafb1d9733cc25a60be03e30822cdd15`。
- 修改前 CodeGraph v1.5.0 通过：185 files / 2,234 nodes / 7,614 edges。
- 首屏 DOM 与样式已按真实业务数据重排：补充门店身份图标，闭店区改为中文业务层级，销售与 KPI 数值增加稳定尺寸状态，操作索引增加业务状态样式；手机基准宽度按约 426 CSS px 还原，并为窄手机、平板、桌面和宽桌面定义显式重排。
- 当前运行文件差异为 `WorkshopOverviewPage.jsx`、`mobile-overview.css` 和 `mobile-overview-390.test.mjs`；本日记为实施证据文件。
- 离线 `pnpm install --offline --frozen-lockfile` 成功，7 个 workspace 项目锁文件已是最新。
- 专项测试 7/7 通过；完整测试 181/181 通过；全工作区 typecheck 通过；5 个工作流、88 条策略验证通过；`git diff --check` 通过。
- 首次完整 build 按预期停在 Preview 源码登记门禁：未提交源码不能生成 `preview-manifest.json`。既定顺序为先完成后置 CodeGraph 和实现提交，再运行 `pnpm version:preview`，提交 Preview 登记后重新执行完整 build。
- 后置 CodeGraph v1.5.0 同步并通过：185 files / 2,234 nodes / 7,602 edges，WAL，up to date；2 个代码文件触发索引更新，节点数保持稳定。
- 实现提交 `00f9cb0b89eac25de1c460eb4cac6f134011d220`；PR #81 的 CI run `30402662562` 中 `secrets` 与 `verify` 均成功。
- PR #81 已普通合并，合并 SHA 为 `6f4658ddaafa19dd7866a354742a3d1b6f397b50`；`feature/cloudflare-workers-d1` 已收敛到该 SHA。
- Cloudflare Preview workflow run `30402838084` 在精确合并 SHA 上成功；Free plan、no billing、Preview-only 与分支头一致性门禁均通过。Worker Version 为 `3071a75f-56e9-4fec-8c87-de035683db69`，公开地址为 `https://bike-ops-preview.geeklightonefish.workers.dev`。
- 独立请求 `/health/live`、`/health/ready`、`/api/v1/meta/version` 与 `/` 均为 HTTP 200；三个身份端点全部收敛到 V5.7.9 / `6f4658ddaafa19dd7866a354742a3d1b6f397b50`，Web shell 加载本次 `index-tfR5TmH4.js` 与 `index-B6a9ChVx.css`。
- 版本保持 V5.7.9；未触发 Staging 或 Production，未修改线上 Production D1。下一步：等待用户人工验收 Preview；未明确接受前不得部署 Production。
