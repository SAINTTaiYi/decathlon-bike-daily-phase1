# Journal · 2026-07-29

## Workshop 首屏参考图设计规格

- 用户要求逐项分析生成首屏效果图，确认每个组件、排版、位置、间隔和实际页面效果，并写入 Workshop 项目 Markdown。
- 决策：手机优先，同时覆盖平板和桌面；忠实还原视觉构图，但全部内容使用项目真实数据；本次仅文档，不修改页面代码、不部署。
- 最新基线：`feature/cloudflare-workers-d1` / `3c0f7b31466dba3c146164d368e08ec4a05a1bfc` / V5.7.9。
- 在隔离工作树 `~/worktrees/workshop-first-screen-layout-spec` 建立文档分支 `docs/generated-first-screen-layout-spec`。
- 修改前 CodeGraph v1.5.0 初始化并通过：185 files / 2,234 nodes / 7,614 edges，WAL，up to date。
- CodeGraph 映射首屏真实结构：`BrandHeader`、`StoreContextCard`、`ClosingStatusCard`、`SalesVehiclesPanel`、`OperationsIndex`、`PickupBoard`，以及 `ActionDock` 和 `useClosingWorkflow` 数据链。
- 参考图 `852 × 1705 px` 按 DPR 2 解释为约 `426 × 852.5 CSS px`；设计规格同时记录原图坐标和 CSS 坐标。
- 新增主规格：`plan/WORKSHOP-FIRST-SCREEN-REFERENCE-SPEC.md`。
- 规格覆盖全局 tokens、一级锚点、每个子元素、真实字段、状态矩阵、手机/平板/桌面重排、可访问性、禁止照搬的生成瑕疵与量化验收门禁。
- 本次无 JSX/CSS/API/数据库/迁移/版本/工作流/部署变更。下一步：远端回读、后置 CodeGraph、Markdown 差异与提交范围验证，然后创建文档 PR；不部署 Preview 或 Production。
