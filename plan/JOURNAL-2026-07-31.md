# Journal — 2026-07-31

## Workshop continuous canvas replacement — local gates passed

- Recovered the rejected multilayer-stage task at clean administrative baseline `46240d79163ace8efea341b3121c21dffd8e7415` and created `feat/workshop-continuous-canvas`.
- Ran the true pre-edit CodeGraph v1.5.0 gate: 192 files / 2,299 nodes / 7,710 edges, up to date.
- Completed interactive specification with the user before editing: no visible separators; invisible semantic anchors remain; ultra-long pale fields drift across modules; no per-module sticky; adjacent modules may coexist; active state switches at 42vh with hysteresis; business outer panels are removed; six original objects become cross-module layers; sparse narrative/curve/trail motifs remain; controls settle in 180–240ms; mobile keeps full-strength effects with portrait-specific trajectories.
- Replaced `useModuleStages` and sticky runway geometry with `useContinuousCanvas` and one normalized page progress plus section-local progress.
- Kept all six business modules mounted and unchanged at the data/event boundary. Added deep-link and browser-history restoration, untransformed navigation geometry, pointer capture during settle, sparse motif visibility envelopes, transparent header, and a lightweight translucent dock.
- Updated `DESIGN.md` and `plan/WORKSHOP-OBSIDIAN-PARALLAX-SPEC.md`; marked the prior Preview evidence as rejected and created `plan/WORKSHOP-CONTINUOUS-CANVAS-PREVIEW-EVIDENCE.md`.
- Final targeted contracts pass 14/14; combined related contracts pass 33/33.
- Final full `pnpm test` passed 198/198 in `workshop-continuous-canvas-final2-tests`; repository typecheck passed in `workshop-continuous-canvas-final-typecheck`; direct Web production build passed after final CSS changes.
- Final CodeGraph post-gate is up to date at 192 files / 2,312 nodes / 7,770 edges. CSS and Markdown remain explicit non-indexed exceptions covered by build/contracts/human review.
- Bounded credential scan found no matches; local Gitleaks is unavailable, so GitHub CI `secrets` remains authoritative.
- `browser-harness` is unavailable; prohibited application-browser and Accessibility fallbacks were not used.
- Public version remains V5.7.9. No Preview redeployment, Staging, Production, or Production D1 mutation has occurred yet.
- Functional commit: `7f1bfae289b3285998317c65698637e16f1bc3a8`.
- Next queue: evidence commit → unchanged-version Preview registration → root build → PR/CI → protected Preview-only deployment → independent public endpoint/identity/security verification → human acceptance.


## Workshop continuous canvas replacement — Preview independently verified

- PR #104 passed `secrets` and `verify` in run `30566318666` and merged normally as `4b3d80f8fa61d07202d98dff5c0b99fa397a8c7d`.
- Protected Preview-only run `30566728980` completed successfully on that exact remote head; frozen install, complete validation, Preview D1 migration gate, deploy, and post-deploy identity checks all passed.
- Preview D1 reported `No migrations to apply`; Staging, Production, and Production D1 were not invoked or changed.
- Preview fingerprint remains `fd30cabc3115ef9e051c9ad6217f4bb07f46c74a33f73040621ed2724742c8e9` across 376 files; public version remains V5.7.9.
- Independent public checks passed for live, ready, version metadata, Web shell, strict security headers, and HTTP 308 to HTTPS; all public identities resolve to `4b3d80f` and `environment=preview`.
- Cloudflare current Deployment is `c630ac34-d4c2-48d8-9aa5-1fa544c073be`; Worker Version `2b4f5301-51ed-49bd-bc4c-dfa120088e8e` (#103) receives 100% traffic and exposes the expected Preview bindings.
- Evidence finalization remains docs-only. Human visual, bidirectional scrolling, deep-link/history, control-settle, 390px, reduced-motion, and forced-colors acceptance is still required because `browser-harness` is unavailable and prohibited fallbacks were not used.
