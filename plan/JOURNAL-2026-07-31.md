# Journal — 2026-07-31

## Workshop continuous canvas replacement — local gates passed

- Recovered the rejected multilayer-stage task at clean administrative baseline `46240d79163ace8efea341b3121c21dffd8e7415` and created `feat/workshop-continuous-canvas`.
- Ran the true pre-edit CodeGraph v1.5.0 gate: 192 files / 2,299 nodes / 7,710 edges, up to date.
- Completed interactive specification with the user before editing: no visible separators; invisible semantic anchors remain; ultra-long pale fields drift across modules; no per-module sticky; adjacent modules may coexist; active state switches at 42vh with hysteresis; business outer panels are removed; six original objects become cross-module layers; sparse narrative/curve/trail motifs remain; controls settle in 180–240ms; mobile keeps full-strength effects with portrait-specific trajectories.
- Replaced `useModuleStages` and sticky runway geometry with `useContinuousCanvas` and one normalized page progress plus section-local progress.
- Kept all six business modules mounted and unchanged at the data/event boundary. Added deep-link and browser-history restoration, untransformed navigation geometry, pointer capture during settle, sparse motif visibility envelopes, transparent header, and a lightweight translucent dock.
- Updated `DESIGN.md` and `plan/WORKSHOP-OBSIDIAN-PARALLAX-SPEC.md`; marked the prior Preview evidence as rejected and created `plan/WORKSHOP-CONTINUOUS-CANVAS-PREVIEW-EVIDENCE.md`.
- Final targeted contracts pass 14/14; combined related contracts pass 33/33.
- Final full `pnpm test` passed 198/198 in `workshop-continuous-canvas-final2-tests`; repository typecheck passed in `workshop-continuous-canvas-final-typecheck`; direct Web production build passed after final CSS changes.
- Final CodeGraph post-gate is up to date at 192 files / 2,312 nodes / 7,770 edges. CSS and Markdown remain explicit non-indexed exceptions covered by build/contracts/human review.
- Bounded credential scan found no matches; local Gitleaks is unavailable, so GitHub CI `secrets` remains authoritative.
- `browser-harness` is unavailable; prohibited application-browser and Accessibility fallbacks were not used.
- Public version remains V5.7.9. No Preview redeployment, Staging, Production, or Production D1 mutation has occurred yet.
- Functional commit: `7f1bfae289b3285998317c65698637e16f1bc3a8`.
- Next queue: evidence commit → unchanged-version Preview registration → root build → PR/CI → protected Preview-only deployment → independent public endpoint/identity/security verification → human acceptance.


## Workshop continuous canvas replacement — Preview independently verified

- PR #104 passed `secrets` and `verify` in run `30566318666` and merged normally as `4b3d80f8fa61d07202d98dff5c0b99fa397a8c7d`.
- Protected Preview-only run `30566728980` completed successfully on that exact remote head; frozen install, complete validation, Preview D1 migration gate, deploy, and post-deploy identity checks all passed.
- Preview D1 reported `No migrations to apply`; Staging, Production, and Production D1 were not invoked or changed.
- Preview fingerprint remains `fd30cabc3115ef9e051c9ad6217f4bb07f46c74a33f73040621ed2724742c8e9` across 376 files; public version remains V5.7.9.
- Independent public checks passed for live, ready, version metadata, Web shell, strict security headers, and HTTP 308 to HTTPS; all public identities resolve to `4b3d80f` and `environment=preview`.
- Cloudflare current Deployment is `c630ac34-d4c2-48d8-9aa5-1fa544c073be`; Worker Version `2b4f5301-51ed-49bd-bc4c-dfa120088e8e` (#103) receives 100% traffic and exposes the expected Preview bindings.
- Evidence finalization remains docs-only. Human visual, bidirectional scrolling, deep-link/history, control-settle, 390px, reduced-motion, and forced-colors acceptance is still required because `browser-harness` is unavailable and prohibited fallbacks were not used.


## Workshop reference-home reconstruction — implementation checkpoint

- Recovered clean branch `feat/workshop-reference-home-v1` at administrative baseline `8ae32f3f0760349cc46a185a2141224fc8c71a28`; true pre-edit CodeGraph v1.5.0 passed at 192 files / 2,312 nodes / 7,816 edges.
- Re-read the supplied 1440 x 22775 reference capture and rebuilt only its spatial rhythm for the authenticated Workshop Overview: warm material-object opening, dark work field, warm measured-data field, photographic connection field, and compact updates ending. No proprietary reference asset, text, logo, font, or source code was copied or traced.
- Preserved workflow-backed date, closing state, KPI, per-scene counts, storage/offline state, version and release data, plus all menu, log, KPI, closing, history, refresh, reopen, export and scene-jump handlers.
- Added self-hosted public-domain obsidian and CC0 mechanic-workbench WebP assets (three responsive outputs, 182,902 bytes total); exact sources, authors, changes, licenses and SHA-256 values are in `apps/web/public/images/ops/SOURCES.md`. Original JPEG files were not committed.
- Replaced obsolete fixed-card pixel contracts with reference-home structure, dynamic-data, responsive, accessibility, license and integrity contracts.
- Updated `DESIGN.md` as a candidate branch pending Preview acceptance. Public version remains V5.7.9; no deployment or database mutation has occurred.
- Next: targeted tests and build, focused fixes, full test/typecheck, post-edit CodeGraph, credential scan, checkpoint, commit/PR/Preview gates.


## Workshop reference-home reconstruction — final local gates

- Updated reference-home contracts pass 11/11; combined Overview/continuous-canvas/motion contracts pass 25/25.
- Direct Web production build passed with 122 modules transformed.
- Initial full test exposed a stale local Hono 4.8.5 link while the frozen lockfile requires 4.12.32; no Worker source differed from baseline. A frozen offline install restored Hono 4.12.32 and the isolated redirect/security contract then passed 5/5.
- Final full `pnpm test`, repository `pnpm typecheck`, and workflow policy checks passed.
- The first post-edit `codegraph init` only reported an existing index and was not counted as a gate. The corrective `codegraph sync` processed 2 changed source files; final CodeGraph v1.5.0 is up to date at 192 files / 2,316 nodes / 7,819 edges. CSS, Markdown and WebP remain explicit non-indexed exceptions covered by contracts, build, source/license readback and SHA tests.
- Bounded credential-pattern scan found no matches. Local Gitleaks is unavailable, so GitHub CI `secrets` remains authoritative.
- `browser-harness` remains unavailable; prohibited application-browser and Accessibility fallbacks were not used. Final visual acceptance remains a human Preview gate.
- No public version increment, Preview deployment, Production deployment or database mutation has occurred.


## Workshop reference-home reconstruction — Preview independently verified

- PR #106 passed `secrets` and `verify` in CI run `30585650844`, then merged normally as `8fa4368b4e82524eb73b4a9ad5721c30c22582c6`.
- Protected Preview-only run `30586076585` completed successfully with Free/no-billing/Preview-only gates, frozen install, complete validation, Preview D1 migration gate, deployment and post-deploy identity checks. Preview D1 reported `No migrations to apply`.
- Cloudflare Deployment `d9dce5a3-ee79-4134-ad97-0fe080b98255` serves Worker Version `b22c48af-fc69-4170-9bd3-9083065d8040` at 100% traffic.
- An independent client observed the previous edge identity once immediately after deployment, so the recovery pipeline stopped without replaying deployment. Later recovery confirmed live, ready, metadata, Web, strict security headers, HTTP 308 and all three exact remote asset hashes on the new merge identity.
- Preview fingerprint remains `d15fb2be7e5ed76764ecc528a32f359252925eab87fc6db5acef2f58a34f846c` / 379 files; public version remains V5.7.9.
- Staging, Production and Production D1 were not invoked or changed. Human visual acceptance remains pending because `browser-harness` is unavailable and prohibited fallbacks were not used.


## Workshop static reference poster v2 — implementation

- Started from clean administrative baseline `69743619ce55796c5683eeb77a96fdfbf1205630` on `feat/workshop-static-reference-v2`; pre-change CodeGraph was up to date at 192 files / 2,316 nodes / 7,819 edges.
- Superseded the prior reference-home Preview candidate with a single static 852 x 1876-inspired industrial poster Overview. The five dedicated business modules and App workflow/handler boundaries remain unchanged.
- Generated `obsidian-orange-cut-900.webp` offline from the documented public-domain obsidian source using Pillow; transparent background, monochrome irregular mineral mask, original orange cut faces; SHA-256 `306c82097e6b912d2cc4da5a907a4d052ca330d532ed44ce3c546229f522e258`. Original JPEG remains uncommitted.
- Overview uses explicit mobile/tablet/desktop geometry, no gradients, viewport-scaled type, animation, transition, keyframes, scroll listener, or smooth scrolling. 390px nominal static content height is 2,025px before the fixed header/dock.
- Tests, final gates, CI and protected Preview-only deployment remain pending at this checkpoint. Production and Production D1 are untouched.

## Workshop static reference poster v2 — final local gates

- Frozen offline install, full repository tests, typecheck, workflow policy checks and direct Web build passed.
- Targeted Overview/continuous/motion contracts passed 21/21.
- Post-edit CodeGraph v1.5.0 is up to date at 192 files / 2,309 nodes / 7,804 edges. CSS, Markdown and WebP are explicit non-indexed exceptions covered by contracts, build, source/license readback and exact SHA.
- Bounded credential scan was clear. Local Gitleaks is unavailable; GitHub CI `secrets` remains authoritative.
- No Preview, Staging, Production or database mutation has occurred at this checkpoint.


## Workshop static reference poster v2 — Preview independently verified

- Functional commit `8e7d4685a8d3063f3e7d583438358a86181e82f0`; PR #108 CI run `30595591583` passed `secrets` and `verify`; normal merge `ea6de842df8e91726b8743ee6d0a56887b33c40d`.
- Protected Preview-only run `30595773003` succeeded with Free/no-billing/Preview-only gates and Preview D1 `No migrations to apply`. Public version remains V5.7.9; fingerprint `50568b5166aba46632073c71ebf1a9b1ee4c86de717162402cd82843dfe69e0f` / 380 files.
- Cloudflare Deployment `0f001613-9600-4e42-956a-000ff8e9ecac`; Worker Version `397bb002-a388-430b-8bab-d1b67261063b` (#105) at 100% traffic.
- Independent live/ready/meta/Web/security/HTTP 308 and remote derived-asset SHA checks passed on merge `ea6de842df8e91726b8743ee6d0a56887b33c40d`. Edge identity converged after about 45 seconds; deployment was not replayed.
- Staging, Production and Production D1 were untouched. Evidence finalization is docs-only; human visual acceptance remains pending.


## Workshop static Overview — residual parallax isolation fix

- Human Preview feedback confirmed that scrolling still moved or scaled Overview content, mineral imagery or background despite the component-local static CSS contract.
- Root cause: the unchanged global `useContinuousCanvas` continued writing motion variables to every semantic module; fixed continuous-canvas background/foreground layers remained visible; `useMotionSystem` still applied GSAP press scale; first-entry `useWorkspaceMotion` still assembled the active Overview.
- Added explicit `data-active-scene` ownership, identity motion values for `pulse`, static CSS invariants for `.workshop-overview-module`, and canvas-layer suppression while Overview is active or in view. Overview controls are excluded from GSAP press motion, jumps into/out of Overview use immediate positioning, and first launch into Overview bypasses workspace assembly.
- Five dedicated business modules keep their existing continuous-canvas behavior and data/handler boundaries.
- Related contracts pass 22/22; direct Web build passes. Frozen offline install, complete tests, typecheck, 88 workflow policies, CodeGraph post-sync and bounded credential scan pass in `static-overview-no-parallax-gates` attempt 1.
- Post-fix CodeGraph v1.5.0 is up to date at 192 files / 2,309 nodes / 7,776 edges. CSS and Markdown remain explicit non-indexed exceptions covered by contracts and build.
- Public V5.7.9 remains unchanged. No deployment or database mutation has occurred for this fix yet.


## Workshop static Overview residual parallax — Preview deployed

- PR #111 CI `30618297506` passed and merged normally as `503851a0df2b1186d5d34413f8d8a8ccecf3bfd8`.
- Protected Preview-only run `30618559036` succeeded, Preview D1 had no migrations, and independent envelope/security checks matched the new identity.
- The static Overview is now isolated from global module transforms, canvas layers, GSAP press motion and first-entry assembly. Five dedicated modules remain unchanged.
- V5.7.9 unchanged; Staging, Production and Production D1 untouched.

## Figure 3 pixel-coordinate Overview correction

- Human feedback: the prior Preview split the screen into visually separate large modules and did not match Figure 3.
- Root cause: the 852 × 1876 reference raster was implemented as a fixed shell header plus four stacked CSS surfaces (`970 + 285 + 590 + 180px`) with independent borders/backgrounds, instead of one poster coordinate field.
- Specification: `plan/WORKSHOP-FIGURE-3-PIXEL-SPEC.md` records component bounds, anchors, spacing, layer order, responsive conversion, data rules and accessibility compensation.
- Implementation: Overview now uses one `aspect-ratio: 852 / 1876` field with source-coordinate percentages/container units; header overlays the same field; mineral/plinth/KPI/photos/card/footer overlap; old duplicate in-poster operation strip and section bands are removed.
- Business preservation: real `workflow.dateKey`, records, sales KPI, closing readiness, online state, menu/log, KPI edit, close/history/refresh, reopen/export and repair/pickup navigation remain wired. Five dedicated modules are unchanged.
- Static guarantee: global canvas is hidden for Overview, identity runtime values remain enforced, and the poster has no animation, transition, smooth scroll, parallax or transform-driven layout.
- Verification: targeted Overview contracts 8/8; regression subset 11/11; full suite 129/129 after scoping the serif face; typecheck passed; 88 workflow-policy checks passed; direct Vite build passed with 122 modules; forbidden old-section scan and `git diff --check` passed; public version remains V5.7.9.
- First full-gate attempt failed only because importing the historical global `noto-serif-sc.css` violated the paper-material contract. It was not retried blindly: the global import was removed and one local Latin variable subset was declared as `Figure 3 Serif`; the failed attempt remains recorded as evidence.
- Preview lifecycle: pending candidate commit, preview manifest, PR/CI and Protected Preview-only deployment. Production and Production D1 remain untouched.

## Static swipe scene navigation and Figure 2 Pickup

- Human feedback removed the persistent bottom navigation and retired the former vertical continuous-scroll scene selector.
- Architecture now mounts exactly one scene at a time through `useStaticSceneNavigation`; URL hashes and browser history remain authoritative, and scene entry always scrolls to top with `behavior: auto`.
- The runtime no longer mounts `ContinuousCanvas`, `useContinuousCanvas`, `useMotionSystem`, `ActionDock`, the six-scene vertical stack, or the closing/footer navigation surface.
- Overview left workbench card accepts a deliberate right swipe to Repair; right workbench card accepts a deliberate left swipe to Pickup. Horizontal intent requires at least 48px and 1.25x the vertical delta. Click and keyboard activation remain available.
- Pickup follows the 852×1839 Figure 2 geometry: real module identity header, chapter marker, licensed mineral and real queue totals, floating tool surface, active summary, and single-column cards. The reference bottom dock is intentionally omitted.
- Existing real behavior is retained: search, source filters, sort, density, collapse, add, edit, operation history, notification state and pickup confirmation. Reference names, phone numbers, dates and tickets are not copied.
- Pickup is static in this phase: no IntersectionObserver entry reveal, scroll auto-hide, card transition, gradient glow, pixel-fill wait, keyframe or page transition. The second waiting record is expanded by default when available, otherwise the first.
- Targeted static/navigation/Overview/Pickup/business tests and direct Vite build passed before full gates. Public version remains V5.7.9; Preview/Production have not yet been changed in this stage.

## Obsidian Assembly continuous-scroll feedback

- Human feedback explicitly reversed the static one-scene architecture: all six modules return to one ordinary native vertical document.
- Reused the audited Obsidian Assembly mechanics without copying brand assets or source: normalized passive-rAF progress, one document-spanning S trajectory, bounded opposing mineral motion, and deterministic transformed-character reveal.
- The #114 Figure 2 Pickup implementation and every workflow handler remain intact. The persistent dock stays removed; no scroll snap, sticky stage, or wheel/touch interception was introduced.


## Full Obsidian Assembly reconstruction
- Scope expanded to all six modules using local REPORT.md SHA `4ca33d2c…`.
- Pre-CodeGraph task `workshop-obsidian-full-codegraph-pre` succeeded.
- Pickup-boundary jitter root cause: per-frame geometry reads plus active-scene CSS reflow. Replaced with cached offsets and invariant shell rules.
- Implemented four-band loader, five sticky material stages, exact character reveal parameters, background line layering, reduced motion, and three CC0 assets.
- Next: frozen full gates, post-CodeGraph, candidate CI and Protected Preview-only deployment.

- Final frozen gates: full repository tests, typecheck, 88 workflow policies, official Web build, CC0 asset integrity and bounded credential scan passed. Post-change CodeGraph: 197 files / 2,327 nodes / 7,808 edges, up to date.

## Full-bleed immersive overlay feedback

- Human feedback: eliminate the split between image/motion and the large module below; full-bleed every image; redistribute all business components over it; match the local Obsidian Assembly vertical replacement mechanics; make gesture coupling faster/lower; use a gradient-transparent sticky header.
- Recovery verified clean baseline `f856774bf7c0be8651caf95e145ce98aab814a13`; no partial write or external side effect existed. Pre-change CodeGraph and REPORT.md analysis were already complete.
- Implemented one full-bleed sticky material scene for all six modules, including Overview; local translucent semantic instruments replace page-sized white modules. Updated scroll mapping to 82% local entry / 62% active identity and retained cached geometry.
- Updated header treatment, Obsidian four-band wipe, reduced-motion and forced-colors fallback. Business data, handlers, dialogs and data models are unchanged.
- Targeted contracts pass 23/23; direct Vite build passes with 122 modules. Full gates, post-CodeGraph, candidate/CI and Protected Preview-only deployment remain pending.
