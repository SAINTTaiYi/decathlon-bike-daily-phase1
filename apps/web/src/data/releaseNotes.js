export const APP_VERSION = "6.8.7"

export const currentRelease = {
  version: APP_VERSION,
  date: "2026.09.19",
  title: "同步链路上游诊断增强",
  summary: "为快速定位门店级上游异常，同步失败时会记录上游返回的状态码与响应说明（仅用于诊断，不含任何账号信息），运维可在不猜测的情况下判断问题出在上游哪一环。本次为后台可观测性改进，界面与操作不变。",
  changes: [
    "同步失败时记录上游调用的 HTTP 状态、请求路径与响应摘要，问题定位从「猜测」变为「直读」",
    "错误码并入上游状态（如 500 / 4xx 一目了然），便于区分「上游内部错误」与「请求被拒」两类故障",
    "本次为后台诊断能力增强，界面与操作不变"
  ]
}
