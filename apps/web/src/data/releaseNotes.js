export const APP_VERSION = "6.7.7"

export const currentRelease = {
  version: APP_VERSION,
  date: "2026.09.12",
  title: "同步任务偶发停摆修复（CPU 预算优化）",
  summary: "修复同步任务偶发被平台资源限制终止导致的看板延迟：消除时区格式化、订单详情重拉与凭据解密中的重复计算，任务 CPU 下降 59%、回到平台预算内",
  changes: [
    "现象：9 月 10 日起同步任务偶发被平台资源限制终止，最长一段停摆 1 小时 45 分——期间自提看板、统计与部分数据更新延迟（恢复后自动补齐，无数据丢失）",
    "根因一：每分钟的营业时间判定重复构造时区格式化器（约 235 微秒/次、每轮约 10 次），单项占任务 CPU 近一半",
    "根因二：完整对账为每个订单重拉详情（每单 2 个上游请求）——上游列表不返回更新时间，导致有变化才拉的判断永久失效",
    "根因三：凭据解密存在重复劳动（token 有效时仍先解密账号密码；同一轮内对同一 token 多次解密）",
    "修复：时区格式化器模块级缓存；订单列表指纹比对（数据未变即跳过详情重拉，状态变化 / 新订单 / 回店订单仍全量刷新）；去掉冗余解密；同一轮同步共享一次连接解析",
    "配套：分类状态读取由先写后读改为先读、缺才写，每分钟少 4 次数据库调用",
    "效果：本地同口径基准任务 CPU 中位数下降 59%，完整对账等重负载轮次同样回落，为平台 CPU 预算留出安全余量",
    "新增 4 例回归测试锁定上述行为（含真实上游服务的行为断言），注入旧写法实测全部变红",
    "全套 804 例零失败（worker 246 / web 510 / api 21 / database 20 / domain 7）"
  ]
}
