name: Retired workshop.skin Staging onboarding

# Retired on 2026-08-12. The canonical domain remains on the existing Staging
# Worker until a separately approved Production cutover is completed.
# No DNS or Worker-domain mutation is performed by this marker workflow.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "Domain onboarding is retired. Production cutover requires a separate approved runbook."
          exit 1
