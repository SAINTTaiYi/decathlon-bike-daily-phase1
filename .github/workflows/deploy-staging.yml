name: Retired legacy EdgeOne staging workflow

# Retired on 2026-08-12. The canonical Staging path is
# deploy-cloudflare-staging.yml and targets Cloudflare Workers + D1.
# This inert workflow remains as an auditable migration marker and cannot deploy.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "This workflow is retired. Use Deploy Cloudflare staging · free stack."
          exit 1
